---
title: Installation
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

Afin d'entrer dans le vif du sujet je vous invite a faire un choix.

Utiliser le code source ou le code PréCompilé :

<Tabs
  groupId="from"
  values={[
    {label: 'Source', value: 'Source'},
    {label: 'PréCompiler', value: 'compiled'}
  ]}>
  <TabItem value="Source">

### Vous avez choisi d'utiliser le code source de l'application

  <p>Un choix honorable mais risquer.</p>
  Avant de continuer veillez à remplir ces prérequis :
  <ul>
    <li> Avoir <a href="https://git-scm.com/">git</a> sur son ordinateur </li>
    <li> Avoir une installation de <a href="https://nodejs.dev/">node</a> v14 ou plus</li>
    <li> Avoir <a href="https://yarnpkg.com/">Yarn</a> ou <a href="https://www.npmjs.com/get-npm">Npm</a> (de préférence Yarn)</li>
  </ul>
  <p>Prêt à commencer ?</p>

#### 1. Récupération du code source

  ```shell
    git clone https://github.com/batleforc/UnlabeledProject.git
    cd UnlabeledProject
  ```

:::info

Première étape, vous venez de récupérer le code source de cette solution sans titre et de vous déplacer dans le dossier les contenant

:::

#### 2. Installation des dépendance

  <Tabs
  groupId="pmanager"
  defaultValue="yarn"
  values={[
    {label:"Yarn", value : "yarn" },
    {label:"Npm", value:"npm" },
  ]}>
  <TabItem
  value="yarn">

  ```shell
    yarn
  ```

  </TabItem>
  <TabItem
  value="npm">

  ```shell
    npm install
  ```

  </TabItem>
  </Tabs>

:::info

Seconde étape, un peu plus longue cette fois si, vous venez d'installer les "outils" nécessaire a l'utilisation de la solution

:::

#### 3. Compilation du code

  <Tabs
  groupId="pmanager"
  defaultValue="yarn"
  values={[
    {label:"Yarn", value : "yarn" },
    {label:"Npm", value:"npm" },
  ]}>
  <TabItem
  value="yarn">

  ```shell
    yarn build
  ```

  </TabItem>
  <TabItem
  value="npm">

  ```shell
    npm run build
  ```

  </TabItem>
  </Tabs>

:::info

Troisième étape, Vous venez de préparer le code a être exécuter

:::

#### 4. Et si on lançais le code ?

  <Tabs
  groupId="pmanager"
  defaultValue="yarn"
  values={[
    {label:"Yarn", value : "yarn" },
    {label:"Npm", value:"npm" },
  ]}>
  <TabItem
  value="yarn">

  ```shell
    yarn S:DevWatch
  ```

  </TabItem>
  <TabItem
  value="npm">

  ```shell
    npm run S:DevWatch
  ```

  </TabItem>
  </Tabs>

:::info

Le code fonctionne quand vous vous rendez sur <http://localhost:5002> ? Alors vous pouvez passer a l'étape suivante !

:::

#### 5. Création des exécutable

  <Tabs
  groupId="pmanager"
  defaultValue="yarn"
  values={[
    {label:"Yarn", value : "yarn" },
    {label:"Npm", value:"npm" },
  ]}>
  <TabItem
  value="yarn">

  ```shell
    yarn dist
  ```

  </TabItem>
  <TabItem
  value="npm">

  ```shell
    npm run dist
  ```

  </TabItem>
  </Tabs>

:::info

Une fois cette commande finis vous retrouverez les exécutable/installer dans le dossier dist. Pour la suite de la marche a suivre je vous invite a retourner tout en haut et cliquer sur PréCompiler

:::

  </TabItem>
  <TabItem
  value="compiled">

### Vous avez choisis d'utiliser les source pré-compiler

:::info

Vous retrouverez les fichier compiler dans l'onglet [Release](/release)

:::

  <Tabs
    groupId="os"
    defaultValue="linux"
    values={[
      {label:"Linux", value : "linux" },
      {label:"Windows", value:"win" },
      {label:"Mac", value:"mac" }
    ]}>
  <TabItem
  value="linux">
  Deux type de fichier sont disponible
    <Tabs
    defaultValue="snap"
    values={[
      {label:".snap", value:"snap" },
      {label:".appImage", value:"appImage" }
    ]} >
  <TabItem
  value="snap">

```shell
  snap install {Le fichier en .snap} --dangerous --classic
```

  Le param dangerous permet d'installer le fichier snap qui n'est pas encore signer. Le param classic permet d'installer le fichier snap en lui laissant l'accès aux fichier du pc (cella permet entre autre d'installer les fichier ffmpeg)
  </TabItem>
  <TabItem
  value="appImage">

```shell
  chmod a+x {Le fichier en .appImage}

  ./{Le fichier en .appImage}
```

  La premiére commande indique que le fichier est exécutable, la seconde permet de l'exécuter
  </TabItem>
  </Tabs>
  </TabItem>
  <TabItem
  value="win">
  Deux type de fichier sont disponible
  <Tabs
    defaultValue="exe"
    values={[
      {label:"Setup.{version}.exe", value:"exe" },
      {label:"portable.{version}.exe", value:"portable" }
    ]} >
    <TabItem
    value="exe">
    Le fichier que vous souhaiter est un installable
    <p>Afin de l'utiliser veuillez double cliquer sur celui ci.</p>
    Une fois l'installation finis, il ne vous reste plus qu'a lancer l'exécutable présent sur votre bureau
    </TabItem>
    <TabItem
    value="portable">
    Le fichier que vous avez choisis contient la version portable de l'application
    <p> Afin d'utiliser cette version il suffit uniquement de la lancer.</p>
    </TabItem>
    </Tabs>
  </TabItem>
  <TabItem
  value="mac">
  Malheureusement pour vous, Mac n'est pas encore complètement supporter, celui-ci nécessite la possession d'un mac pour compiler l'application.
  <h4>Heureusement pour vous (et moi), vous pouvez toujours utiliser ce projet à partir des sources !!</h4>
  </TabItem>
  </Tabs>
  </TabItem>
</Tabs>

### Possible erreur

#### FFMPEG unavailable

![Ffmpeg](/img/ffmpeg.png)

Le message que vous venez de voir est commun aux démarage et contient une bonne partie des information nécessaire a sa résolution.

Pas de panique je suis la pour vous aider dans sa résolution.

Peux importe votre Systéme d'exploitation le processus est le même:

1. Allez sur <https://ffmpeg.org/download.html> (vous pouvez aussis cliquer directement sur le lien dans la modal, celui ci vous dirigera vers la page de téléchargement)
2. Télécharger l'archive contenant le build correspondant a votre systéme.
3. Ensuite veuillez placer le fichier ffmpeg contenue dans l'archive dans le dossier indiquer en derniére ligne de la modal
4. Cliquer sur try again, si la modal disparais vous être prêt a passer a l'étape suivante

Si vous rencontrez un probléme veuillez me contacter par <a href="mailto:maxleriche.60@gmail.com">mail</a> ou ouvrez une issue sur <a href="https://github.com/batleforc/UnlabeledProject/issues">github</a>.

### Que c'est t'il passer ?

Pourquoi avoir choisis un client sous cette forme ?

Deux chose sont a savoir:

- Le client est uttilisable via votre navigateur ou le client, certaine fonctionnalité telle que la persistance du client en ten que service d'arriére plan (que je sais possible) est une fonctionalité a venir. l'uttilisateur n'aura a terme qu'a lancer son navigateur ou a juste double cliquer sur l'icone dans le volet de notification/ ou encore a relancer l'executable
- A terme une version serveur sera disponible.

Pourquoi avoir fait le choix de ne pas Télécharger de maniére procédural ffmpeg ?

Le probléme avec un téléchargement procédural est le risque que celui ci écrase une version qui serais fournis pas l'uttilisateur.

J'ai (suite aux expliquation d'un de mes professeur) commencer a uttiliser ffmpeg dans d'autre script pour l'uniformisations des audio fournis aux bot. l'assurance que ffmpeg sera dans ce dossier et y restera me permetra a l'avenir d'avancer sur une fonctionnalité de cache des audio.
